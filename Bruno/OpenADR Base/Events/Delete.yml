info:
  name: Delete
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BASE_URI}}/events/0"
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
