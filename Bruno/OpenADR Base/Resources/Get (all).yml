info:
  name: Get (all)
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URI}}/resources"
  params:
    - name: venID
      value: "{{venID}}"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
